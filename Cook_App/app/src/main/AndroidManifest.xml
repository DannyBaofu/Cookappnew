<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.agmostudio.cook_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".deliverypage"></activity>
        <activity android:name=".ordercustomerpage" />
        <activity android:name=".menulist" />
        <activity android:name=".profileinfo" />
        <activity android:name=".orderinformation" />
        <activity android:name=".Home_Page" />
        <activity android:name=".order_page" />
        <activity android:name=".signuppage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>